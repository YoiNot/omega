{
  "name": "omega",
  "version": "0.1.0",
  "private": true,
  "description": "PROJECT OMEGA — an autonomous, architecturally-sound browser game engine and simulation. Monorepo.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "bench": "vitest bench --run",
    "lint": "tsc -b --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.7"
  },
  "engines": {
    "node": ">=20"
  }
}
