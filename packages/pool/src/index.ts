/**
 * @omega/pool — deterministic memory pool / buffer allocator.
 */

export {
  PoolAllocator,
} from './allocator.js';

export type {
  PoolOptions,
  AllocHandle,
  PoolSnapshot,
} from './allocator.js';
